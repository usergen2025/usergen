# Python Background Removal Dependencies
# Note: Python 3.8-3.13 is recommended. Python 3.14+ may not be compatible with onnxruntime.
# If onnxruntime installation fails, try installing without version constraints or use Python 3.13 or earlier.

rembg>=2.0.50
# onnxruntime is a dependency of rembg, but may need to be installed separately on some systems
# If installation fails, rembg will attempt to install it automatically
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0

